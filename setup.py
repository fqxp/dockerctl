#! /usr/bin/env python

from distutils.core import setup

setup(name='dockerctl',
      version='0.1',
      py_modules=['dockerctl'],
      scripts=['dockerctl'],
      author='Frank Ploss',
      author_email='frank@fqxp.de',
      )

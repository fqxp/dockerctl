# Automatically added by dh_installinit
if [ -x "/etc/init.d/dockerctl" ]; then
	update-rc.d dockerctl defaults >/dev/null
	invoke-rc.d dockerctl start || exit $?
fi
# End automatically added section
